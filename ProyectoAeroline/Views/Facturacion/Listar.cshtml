@model IEnumerable<ProyectoAeroline.Models.FacturacionModel>
@using ProyectoAeroline.Helpers

@{
    ViewData["Title"] = "Listar Facturación";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<div class="container-fluid px-4 mt-4 animate__animated animate__fadeInUp">
    <div class="card shadow-lg border-0 rounded-4">
        <div class="card-header bg-gradient text-white py-3 rounded-top-4 d-flex justify-content-between align-items-center header-glow">
            <h3 class="mb-0">
                🧾 <i class="bi bi-receipt-cutoff"></i> <strong>Lista de Facturas</strong>
            </h3>
            @if (PermisosHelper.PuedeCrear(ViewContext, "Facturacion"))
            {
                <a href="/Facturacion/Guardar" class="btn btn-light btn-sm fw-bold fancy-btn">
                    ➕ <i class="bi bi-plus-circle"></i> Agregar
                </a>
            }
        </div>

        <div class="card-body bg-light">
            <table id="tablaFacturacion" class="table table-hover align-middle text-center w-100">
                <thead class="table-primary">
                    <tr>
                        <th>🆔 ID</th>
                        <th>🎫 Boleto</th>
                        <th>📅 Fecha Emisión</th>
                        <th>🕐 Hora</th>
                        <th>📝 Descripción</th>
                        <th>💳 Tipo Pago</th>
                        <th>💱 Moneda</th>
                        <th>💵 Monto</th>
                        <th>📊 Impuesto</th>
                        <th>🧾 Monto Factura</th>
                        <th>💰 Monto Total</th>
                        <th>🔖 N° Autorización</th>
                        <th>⚙️ Estado</th>
                        <th>🪄 Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.IdFactura</td>
                            <td>@item.IdBoleto</td>
                            <td>@item.FechaEmision.ToString("dd/MM/yyyy")</td>
                            <td>@(item.HoraEmision?.ToString(@"hh\:mm") ?? "N/A")</td>
                            <td>@item.Descripcion</td>
                            <td>@item.TipoPago</td>
                            <td>@item.Moneda</td>
                            <td>@(item.Monto?.ToString("N2") ?? "0.00")</td>
                            <td>@(item.Impuesto?.ToString("N2") ?? "0.00")</td>
                            <td><strong>@item.MontoFactura.ToString("N2")</strong></td>
                            <td><strong>@(item.MontoTotal?.ToString("N2") ?? "0.00")</strong></td>
                            <td>@item.NumeroAutorizacion</td>
                            <td>
                                <span class="badge @(item.Estado == "Activo" ? "bg-success" : item.Estado == "Cancelado" ? "bg-danger" : "bg-secondary") px-3 py-2 rounded-pill shadow-sm">
                                    @(item.Estado == "Activo" ? "🟢 Activo" : item.Estado == "Cancelado" ? "🔴 Cancelado" : "⚫ Otro")
                                </span>
                            </td>
                            <td>
                                <div class="btn-group" role="group">
                                    @if (PermisosHelper.PuedeEditar(ViewContext, "Facturacion"))
                                    {
                                        <a href="@Url.Action("Modificar", "Facturacion", new { CodigoFactura = item.IdFactura })"
                                           class="btn btn-outline-primary btn-sm fancy-action" title="Editar Factura">
                                            🛠️ <i class="bi bi-pencil-square"></i>
                                        </a>
                                    }
                                    @if (PermisosHelper.PuedeEliminar(ViewContext, "Facturacion"))
                                    {
                                        <button type="button" class="btn btn-outline-danger btn-sm fancy-action"
                                                onclick="confirmarEliminacion('@item.IdFactura', '@item.IdFactura')"
                                                title="Eliminar Factura">
                                            🗑️ <i class="bi bi-trash"></i>
                                        </button>
                                    }
                                    @if (!PermisosHelper.PuedeEditar(ViewContext, "Facturacion") && !PermisosHelper.PuedeEliminar(ViewContext, "Facturacion"))
                                    {
                                        <span class="text-muted">Sin acciones disponibles</span>
                                    }
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

@section Scripts {
    <!-- Animate.css -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" />

    <!-- SweetAlert2 -->
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    <!-- DataTables -->
    <script src="https://cdn.datatables.net/1.13.7/js/jquery.dataTables.min.js"></script>
    <link rel="stylesheet" href="https://cdn.datatables.net/1.13.7/css/jquery.dataTables.min.css" />

    <script>
        $(document).ready(function () {
            $('#tablaFacturacion').DataTable({
                language: {
                    url: 'https://cdn.datatables.net/plug-ins/1.13.7/i18n/es-ES.json'
                },
                pageLength: 10,
                lengthMenu: [5, 10, 25, 50],
                responsive: true,
                ordering: true,
                columnDefs: [
                    { orderable: false, targets: [13] }
                ]
            });
        });

        function confirmarEliminacion(id, numero) {
            Swal.fire({
                title: '¿Eliminar Factura?',
                html: `🧾 La factura <b>#${numero}</b> será eliminada permanentemente.`,
                icon: 'warning',
                showCancelButton: true,
                confirmButtonColor: '#d33',
                cancelButtonColor: '#3085d6',
                confirmButtonText: 'Sí, eliminar',
                cancelButtonText: 'Cancelar',
                backdrop: 'rgba(0,0,0,0.4)'
            }).then((result) => {
                if (result.isConfirmed) {
                    window.location.href = `/Facturacion/Eliminar?CodigoFactura=${id}`;
                }
            });
        }
    </script>

    <style>
        .bg-gradient {
            background: linear-gradient(135deg, #007bff, #00b4d8);
        }

        .header-glow {
            box-shadow: 0 0 25px rgba(0, 174, 255, 0.6);
        }

        .fancy-btn,
        .fancy-action {
            border-radius: 25px;
            font-weight: bold;
            transition: all 0.3s ease-in-out;
        }

            .fancy-btn:hover {
                transform: scale(1.1);
                box-shadow: 0 0 20px rgba(0, 174, 255, 0.6);
            }

            .fancy-action:hover {
                transform: scale(1.1);
                box-shadow: 0 0 15px rgba(255, 255, 255, 0.3);
            }

        table thead {
            background: linear-gradient(135deg, #90e0ef, #0077b6);
            color: white;
            text-shadow: 0 0 5px rgba(0, 0, 0, 0.2);
        }

        table tbody tr:hover {
            background-color: #caf0f8 !important;
            transform: scale(1.01);
            transition: 0.2s ease-in-out;
        }

        .card {
            transition: 0.4s ease;
        }

            .card:hover {
                transform: scale(1.02);
                box-shadow: 0 0 25px rgba(0, 123, 255, 0.3);
            }

        .badge {
            font-size: 0.9rem;
            letter-spacing: 0.5px;
        }
    </style>
}
